import { Quote } from './quote';

describe('Quote', () => {
  it('should create an instance', () => {
    expect(newQuote()).toBeTruthy();
  });
});
function newQuote(): any {
  throw new Error('Function not implemented.');
}

